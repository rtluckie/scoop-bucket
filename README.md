# scoop-bucket

Scoop bucket for releases from multiple projects.

Each project lives under its own namespace in `bucket/`, e.g. `bucket/rtluckie/vizid/vizid.json`.

## Install

```
scoop bucket add rtluckie https://github.com/rtluckie/scoop-bucket
scoop install gvizid
```
